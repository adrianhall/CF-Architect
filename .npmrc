# ignore-scripts is suspended during active development (Phases 01–11).
# It will be re-instated in Phase 12 (Security Hardening) once all packages
# have been audited and the postinstall allowlist has been finalised.
# See docs/plan/phase-12.md.
engine-strict=true
fund=false
audit-level=high
