/**
 * apps/worker/src/db/schema.ts
 *
 * Drizzle ORM schema definition.
 *
 * Phase 01: Empty — establishes the schema file and Drizzle infrastructure
 * without defining any tables. Tables are added in Phase 02+ as features
 * require them.
 *
 * See PLAN.md §6 for the planned table definitions.
 */

// Tables will be exported from this file as they are added.
// Example (Phase 02):
//   export const users = sqliteTable("users", { ... });
